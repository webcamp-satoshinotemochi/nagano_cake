<div class="container">
 <div class="row">
  <div class="col m-3">
    <h2>注文履歴一覧</h2><br>
    <table class= "table">
      <thead>
        <tr>
          <th scope="col">購入日時</th>
          <th scope="col">購入者</th>
          <th scope="col">注文個数</th>
          <th scope="col">注文ステータス</th>
        </tr>
      </thead>
      <tbody>
        <% @order_items.each do |order| %>
        <tr>
          <th scope="low"><%= order.created_at %></th>
          <th scope="low"><%= order.name %></th>
          <th scope="low"><%= order.quantity %></th>
    	  <th scope="low"><%= order.status %></th>
    	  <th></th>
    	 </tr>
    	 <% end %>
      </tbody>
    </table>
  </div>
 </div>
</div>