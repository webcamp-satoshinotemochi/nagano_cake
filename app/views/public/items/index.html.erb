<div class="container px-5 px-sm-0">
    <div class="row">
        <div class="col-3">
            <%= render "genre", genre: @genres %>
        </div>
        <div class="col-9">
            <h2>商品一覧<span class="h4">（全 <%= @total_items.count %>件）</span></h2>
            <div class="container">
                <div class="row row-cols-1 row-cols-sm-2 row-cols-md-4 row-eq-height">
                    <% @items.each do |item| %>
                      <div class="col">
                        <div class="card mx-auto mt-4">
                            <%= link_to item_path(item) do %>
                                <%= attachment_image_tag item, :image, size:"200x200", class:"card-img-top", fallback:"no-img.png" %>
                            <% end %>
                            <div class="card-body">
                                <div class="card-title">
                                    <%= link_to item.name, item_path(item), class:"text-dark" %>
                                </div>
                                <div class="card-text">
                                    <%= "¥"+(item.price_before_tax ).to_s(:delmited) %>
                                </div>
                            </div>
                        </div>
                      </div>
                    <% end %>
                </div>
            </div>
            <div class="d-flex pagenation justify-content-center mt-3">
                <%= paginate @items %>
            </div>
        </div>
    </div>
</div>