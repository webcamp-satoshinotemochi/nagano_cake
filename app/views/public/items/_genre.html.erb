<div class="border border-3 px-3 py-3">
  <tabele class="tabele tabele-borderless">
    <thed>
      <tr>
        <th class="text-center">ジャンル検索</th>
      </tr>
    </thed>
    <tbody>
      <% genre.each do |genre| %>
        <tr>
          <td>
            <%= genre.name %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </tabele>
</div>